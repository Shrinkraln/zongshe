# 第4课 提交目录（sub/）· G42

> 按 task_plan T6.2「归档清单」整理：失效模式记录表、三条边界声明、全部录像、导航日志 CSV、预注册单、回归记录、AI辅助使用记录表。整理时间：2026-09-22；对应 git 提交：`1fa5796` / `8576732` / `6a29db6`。

## 提交物清单与状态

| 归档清单项 | 文件 | 状态 |
|---|---|---|
| 失效模式记录表 | `失效模式记录表.md` | ① 完稿；②③④ 已写（②③ 标"补测"、④ 如实记目标点偏差与无录像）；实车两行待课堂抄录 |
| 三条边界声明 | `能力边界声明.md` | 三条候选稿（RPP 停线内目标 / 随车成墙障碍 / AMCL 漂移"到达"≠"真到达"），待组员互问定稿签字 |
| 全部录像 | `videos/`（3 段：虚拟障碍 / 遮挡30% / 定位扰动，均失败段按纪律归档） | ✓（①②③ 段；④ 无录像以日志+traj 取证） |
| 导航日志 CSV | `logs/`（10 份 traj + `轨迹元数据.md` 统计与 sha256 索引） | ✓（③ 的 U1 复测与②③补测 CSV 待课后追加） |
| 预注册单 | `预注册单_第4课.md` | 假设/阈值/冻结 SHA（a57b6753…）已填；成员/日期/签字、最窄通道实测值待补 |
| 回归记录 | —（课后必做） | 改过膨胀 → 复跑 run_action1，得分回写预注册单「变更登记」#2 后补入本目录 |
| AI辅助使用记录表 | `AI辅助使用记录表_第4课.md` | 7 行（含 #3 navigate_to 三缺陷、#6 注入工具 DDS 发现延迟缺陷） |

## 支撑材料（非归档清单，供复核）

| 文件 | 用途 |
|---|---|
| `config/nav2_params_sim.yaml` | 冻结基线参数（SHA a57b6753…，四项核对 P14） |
| `config/nav2_params_inj1.yaml` / `nav2_params_inj2.yaml` | ①② 注入参数副本（仅 scan 话题 remap，diff 各 2 行） |
| `scripts/inject_pose_bias.py` | ③ 注入一键工具（已修复 DDS 发现延迟缺陷，实测送达） |
| `nav2_ws_src/` | navigate_to 接口与服务源码（验收③ 20%，三缺陷修复版） |

## 待办（课后/课堂，详见任务目录 progress.md 待办）

1. ② 补测（inj2 重启栈四步流程）、③ 补测（主试重跑 `traj_G42_inject3_amcl_v2.csv` + U1 复测 `_repeat.csv`）→ 完成后补记录表行与 CSV/录像，同步本目录
2. 实车两行抄录（课堂 T4.1）＋签字
3. 边界声明互问定稿；预注册单签字；回归 run_action1 并回填
4. T6.1 验收三项核对 + T6.2 最终归档

**校验**：全部文件与任务目录原件 sha256 一致（`sha256sum` 可复核）；任务目录为唯一编写处，本目录为提交快照——后续修改先回任务目录再同步。
